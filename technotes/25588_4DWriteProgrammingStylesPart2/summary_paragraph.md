This Tech Note is Part II of the "4D Write, Programming Styles, and Entry Control" series (Part I is also in this batch), demonstrating how to control a 4D Write word-processing area to build a hierarchical list structure directly within a document. It notes that, depending on how the technique is used, the developer may need to store the underlying list data using the BLOB to list and LIST TO BLOB commands, which are useful for reusing the list data in databases that do not themselves use 4D Write. Only the short on-page teaser text could be recovered here, so the specific styling/entry-control code is not available, but this reflects a practical, somewhat advanced use of 4D Write's programmatic capabilities beyond basic text entry.
