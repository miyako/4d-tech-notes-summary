This Tech Note explains that 4D Open allows a user to Add, Modify, and Delete records on a 4D Server, and focuses specifically on a simple way to add records remotely using the plug-in, completing the basic CRUD-style series covered across this batch (alongside the companion update and delete notes). Only the short on-page teaser text could be recovered here, so the specific commands and code used to add remote records are not available, but this note establishes the baseline operation that the update and delete notes build on.
