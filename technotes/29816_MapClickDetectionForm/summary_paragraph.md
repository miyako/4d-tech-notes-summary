This Tech Note tackles a classic UI problem: standard form objects are rectangular, but a picture such as a map is visually divided into irregular regions (e.g., individual US states) that a developer may want to be independently clickable. It describes a technique for detecting which non-rectangular region — an oval or, in this case, a state on a US map — the user clicked on, likely relying on pixel color/region matching or an overlay of invisible clickable zones given the era's toolset. Only the short on-page teaser text could be recovered here, so the exact hit-detection algorithm is not available, but this is a good example of classic 4D form-object creativity used to build interactive, image-based navigation before richer client-side UI frameworks existed.
